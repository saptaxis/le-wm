dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 10
  name: lunarlander_heuristic
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
  cache_dir: /home/vsr/vsr-tmp/lewm-datasets
