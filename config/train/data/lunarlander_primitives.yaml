dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 1
  name:
    - lunarlander_free-fall
    - lunarlander_ground-stationary
    - lunarlander_ground-liftoff
    - lunarlander_ground-side-thrust
    - lunarlander_ground-thrust-sweep
    - lunarlander_impulse-main
    - lunarlander_impulse-side
  keys_to_load:
    - pixels
    - action
  keys_to_cache:
    - action
  cache_dir: /home/vsr/vsr-tmp/lewm-datasets
